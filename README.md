# 🎓 Система управління розкладом в університеті

Цей проєкт реалізує базову систему керування розкладом занять університету з використанням **TypeScript**, **Union Types**, **Type Aliases** та **масивів даних**.  
Всі функції та структури суворо типізовані згідно з вимогами завдання.

---

## 📌 Основна ідея

Система дозволяє:

- зберігати інформацію про викладачів, аудиторії, курси та заняття;
- додавати заняття тільки без конфліктів;
- виявляти конфлікти (викладач/аудиторія);
- знаходити вільні аудиторії;
- отримувати розклад викладача;
- змінювати або видаляти заняття;
- аналізувати завантаженість аудиторій та популярність типів занять.

---

## 📁 Структура типів

### Дні тижня (Union Type)
```
"Monday" | "Tuesday" | "Wednesday" | "Thursday" | "Friday"
```

### Часові слоти (Union Type)
```
"8:30-10:00" | "10:15-11:45" | "12:15-13:45" | "14:00-15:30" | "15:45-17:15"
```

### Тип заняття
```
"Lecture" | "Seminar" | "Lab" | "Practice"
```

### Викладач
```
{ id: number; name: string; department: string }
```

### Аудиторія
```
{ number: string; capacity: number; hasProjector: boolean }
```

### Курс
```
{ id: number; name: string; type: CourseType }
```

### Заняття
```
{
  id: number;
  courseId: number;
  professorId: number;
  classroomNumber: string;
  dayOfWeek: DayOfWeek;
  timeSlot: TimeSlot;
}
```

---

## 📦 Масиви даних

- `professors: Professor[]`
- `classrooms: Classroom[]`
- `courses: Course[]`
- `schedule: Lesson[]`

---

## 🔧 Реалізовані функції

### ✔ Додавання професора
```
addProfessor(prof: Professor): void
```

### ✔ Додавання заняття з перевіркою конфліктів
```
addLesson(lesson: Lesson): boolean
```

### ✔ Пошук вільних аудиторій
```
findAvailableClassrooms(timeSlot, day): string[]
```

### ✔ Розклад викладача
```
getProfessorSchedule(professorId): Lesson[]
```

### ✔ Валідація конфліктів
```
validateLesson(lesson): ScheduleConflict | null
```

### ✔ Завантаженість аудиторії (%)
```
getClassroomUtilization(classroomNumber): number
```

### ✔ Найпопулярніший тип заняття
```
getMostPopularCourseType(): CourseType
```

### ✔ Перенесення заняття в іншу аудиторію
```
reassignClassroom(lessonId, newClass): boolean
```

### ✔ Скасування заняття
```
cancelLesson(lessonId): void
```

---

## 🛠 Використані можливості TypeScript

- **Union Types**
- **Type Aliases**
- **Строга типізація всіх функцій**
- **Масиви з кастомними типами**
- **Перевірки конфліктів через тип ScheduleConflict**

---

## 🎯 Висновок

Проєкт повністю реалізує поставлене завдання:
- структури даних визначені через Type Aliases;
- використовуються Unit Types;
- функції перевіряють конфлікти та працюють з масивами;
- присутній базовий аналіз даних;
- виконана модифікація та видалення занять.

Проєкт готовий до захисту.

