# 🎓 Система управління розкладом в університеті

Проєкт реалізований на TypeScript із застосуванням **union types**, **type aliases**, **масивів**, роботи з DOM та API.  
Мета — створити мінімальну, але функціональну систему управління розкладом занять.

---

## 📌 Мета проєкту

Створити систему, яка дозволяє:

- додавати викладачів;
- створювати заняття з перевіркою конфліктів;
- переглядати зайнятість аудиторій;
- знаходити вільні аудиторії;
- переглядати розклад викладачів;
- аналізувати популярні типи занять;
- змінювати аудиторії;
- видаляти заняття;
- відображати дані на сайті через GitHub Pages.

---

## 🧩 Використані технології

- TypeScript  
- HTML + CSS  
- Fetch API  
- Git & GitHub Pages  

---

## 📁 Структура проєкту

```
/
├── index.html
├── styles.css
├── tsconfig.json
├── README.md
│
├── src/
│   └── main.ts
│
└── dist/
    └── main.js   (створюється після компіляції)
```

---

## 🔠 Визначені типи (Union Types та Type Aliases)

### Дні тижня
```ts
type DayOfWeek = "Monday" | "Tuesday" | "Wednesday" | "Thursday" | "Friday";
```

### Часові слоти
```ts
type TimeSlot =
  "8:30-10:00" |
  "10:15-11:45" |
  "12:15-13:45" |
  "14:00-15:30" |
  "15:45-17:15";
```

### Типи занять
```ts
type CourseType = "Lecture" | "Seminar" | "Lab" | "Practice";
```

### Викладач
```ts
type Professor = {
  id: number;
  name: string;
  department: string;
};
```

### Аудиторія
```ts
type Classroom = {
  number: string;
  capacity: number;
  hasProjector: boolean;
};
```

### Курс
```ts
type Course = {
  id: number;
  name: string;
  type: CourseType;
};
```

### Заняття
```ts
type Lesson = {
  id: number;
  courseId: number;
  professorId: number;
  classroomNumber: string;
  dayOfWeek: DayOfWeek;
  timeSlot: TimeSlot;
};
```

---

## 📚 Масиви даних

```ts
const professors: Professor[] = [];
const classrooms: Classroom[] = [];
const courses: Course[] = [];
const schedule: Lesson[] = [];
```

---

## ⚙ Основні функції

### ➕ Додавання викладача
```ts
addProfessor(professor: Professor): void
```

### ➕ Додавання заняття
```ts
addLesson(lesson: Lesson): boolean
```

### 🔎 Пошук вільних аудиторій
```ts
findAvailableClassrooms(timeSlot: TimeSlot, day: DayOfWeek): string[]
```

### 🧑‍🏫 Розклад викладача
```ts
getProfessorSchedule(id: number): Lesson[]
```

### ⚠ Перевірка конфліктів
```ts
validateLesson(lesson: Lesson): ScheduleConflict | null
```

### 📊 Завантаженість аудиторії
```ts
getClassroomUtilization(classroomNumber: string): number
```

### ⭐ Найпопулярніший тип занять
```ts
getMostPopularCourseType(): CourseType
```

### 🔁 Зміна аудиторії заняття
```ts
reassignClassroom(lessonId: number, newNumber: string): boolean
```

### ❌ Видалення заняття
```ts
cancelLesson(id: number): void
```

---

## 🌐 Інтерактивність на сайті

Використано:

- модальне вікно (open / close)
- подію **scroll**
- подію **click**
- завантаження постів через **fetch API**
- динамічне додавання елементів у DOM
- компіляцію TS → JS

---

## 🛠 Налаштування TypeScript

Файл `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "module": "commonjs",
    "strict": true,
    "rootDir": "src",
    "outDir": "dist",
    "lib": ["ES2017", "DOM"]
  }
}
```

---

## ▶ Як запустити проєкт

### 1. Встановити залежності
```bash
npm install
```

### 2. Зібрати TypeScript
```bash
npx tsc
```

### 3. Відкрити сайт
Відкрити `index.html` або перейти на GitHub Pages.

---

## 🌍 GitHub Pages

Сторінка доступна за посиланням:

```
https://<your-username>.github.io/<repo-name>/
```

---

## 🎯 Висновок

Реалізовано:

- Union Types  
- Type Aliases  
- Роботу з масивами  
- Перевірку конфліктів  
- Аналіз розкладу  
- Інтерактивність сторінки  
- Повну систему управління розкладом на TypeScript  

Проєкт повністю відповідає вимогам.
